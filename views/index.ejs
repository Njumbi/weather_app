<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="styles.css" rel="stylesheet">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css"
        integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <title>
        Weather App
    </title>
</head>

<body>
    <div class="container">
        <div class="header">
            <div class="icon-container">
                <canvas id="icon" width="100" height="100"></canvas>
            </div>
        </div>
        <div class="content">
            <div class="general-information">
                <div class="status"> Enter A Location</div>
                <div class="location"> To find The Weather</div>
            </div>
            <div class="detail-section">
                <div class="detail">
                    <div class="title">Humidity</div>
                    <% if (data) { %>
                    <div class="value"> <%=data.main.humidity %> </div>
                    <%} %>


                </div>
                <div class="detail bordered">
                    <div class="title">Temperature</div>
                    <% if (data) { %>
                    <div class="value"> <%=data.main.temp %></div>
                    <% } %>
                </div>
                <div class="detail">
                    <div class="title">Clouds</div>
                    <% if (data) { %>
                    <div class="value"><%=data.clouds.all %></div>
                    <% } %>
                </div>
            </div>
            <form method="POST">
                <div class="row">
                    <div class="city-search-container">
                        <input type="text" class="city-search" name="city">
                    </div>
                    <button type="send" id="myButton" class="btn btn-lg">Send</button>
                </div>
            </form>
        </div>
    </div>
    </div>
</body>

</html>